<%= form @entry do |f| %>
<%= f.label :user %>
	<%= f.text_field :user %>
	<%= f.label :photo_url %>
	<%= f.text_field :photo_url %>
	<%= f.label :comment %>
	<%= f.text_field :comment%>
	<%= f.submit "Update Entry", id: "submit" %>
<% end %>
